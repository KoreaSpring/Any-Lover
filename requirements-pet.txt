# 桌宠 only 后端的最小 Python 依赖（CPU-only，Windows x64，Python 3.11）。
# 版本对齐 Open-LLM-VTuber 的锁定结果，剔除 torch/whisper/funasr 等重依赖。
# 用途：创建干净环境后 pip install -r requirements-pet.txt，再用 build-backend.js 冻结。

# Web 服务
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.21
websockets==16.0
websocket-client==1.9.0
httpx==0.28.1

# 配置与工具
pydantic==2.12.5
pyyaml==6.0.3
ruamel.yaml==0.18.10
chardet==5.2.0
loguru==0.7.3
tqdm==4.67.1
requests==2.32.5
numpy==1.26.4
scipy==1.17.0
soundfile==0.13.1
pysbd==0.3.4
langdetect==1.0.9

# 本地 ASR（Sherpa SenseVoice，CPU）
sherpa-onnx==1.10.46
onnxruntime==1.23.2

# TTS
edge-tts==7.2.7
# pydub：把 edge_tts 的 mp3 转成前端所需 wav（解 mp3 需 ffmpeg，整合版随包提供）
pydub==0.25.1

# 大模型（OpenAI 兼容）
openai==2.15.0

# 以下 4 个包并非桌宠功能所需，但上游后端在启动链路上「无条件」顶层导入了它们，
# 缺任何一个都会让冻结后的 aibot-backend.exe 直接崩在 import 阶段：
#   service_context.py -> mcpp/mcp_client.py            -> mcp
#   agent_factory.py   -> agents/letta_agent.py         -> letta_client
#   agent_factory.py   -> stateless_llm_factory.py
#                         -> stateless_llm_with_template -> jinja2
#                         -> claude_llm                  -> anthropic
# 版本与上游 backend/uv.lock 锁定结果保持一致。
mcp==1.26.0
anthropic==0.76.0
jinja2==3.1.6
letta-client==1.7.6

# 打包
pyinstaller==6.11.1
