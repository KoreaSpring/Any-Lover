{
  "name": "any-lover-workspace",
  "version": "0.1.0",
  "description": "Any-Lover（角色 Charis）桌宠版 AI 伴侣工作区：frontend（Electron 外壳 + Live2D 前端 + 设置窗口）、backend（上游 Python，黑盒）、site（Vue 官网）、build/scripts（后端组装与冻结）。",
  "author": "ai-bot",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prepare-runtime": "node build/scripts/prepare-runtime.js",
    "build:backend": "node build/scripts/build-backend.js",
    "install:app": "npm --prefix frontend install",
    "install:all": "npm run install:app && npm --prefix site install",
    "python:deps": "python -m pip install -r requirements-pet.txt",
    "dev:setup": "npm run install:app && npm run prepare-runtime",
    "dev": "npm run prepare-runtime && npm run frontend:dev",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "site:dev": "npm --prefix site run sync:live2d && npm --prefix site run dev",
    "site:build": "npm --prefix site run sync:live2d && npm --prefix site run build",
    "pack": "node build/scripts/pack.js",
    "pack:full": "node build/scripts/pack.js --with-ollama",
    "dist": "npm run prepare-runtime && npm run build:backend && npm run pack",
    "dist:full": "npm run prepare-runtime && npm run build:backend && npm run pack:full"
  }
}
